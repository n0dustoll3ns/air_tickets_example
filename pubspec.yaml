name: air_tickets
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.1 <3.0.0'

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  flutter_svg: ^1.1.5
  syncfusion_flutter_datepicker: ^20.3.47

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  assets:
    - assets/journey.svg
    - assets/loupe.svg
    - assets/plane_down_icon.svg
    - assets/plane_up_icon.svg
    - assets/plane.svg
    - assets/swap_direction.svg
    - assets/calendar_start.svg
    - assets/calendar_end.svg

  uses-material-design: true
